az acr commands
25-Sep-2025

01.
Azure ML
Mon,25-Aug

ACR_REGISTRY			9ebc

az acr --name 9ebc
az acr --name 9ebc --expose-token

az acr --name $ACR_REGISTRY
az acr --name $ACR_REGISTRY --expose-token

az acr repository list -n $ACR_REGISTRY
az acr repository list --name $ACR_REGISTRY
az acr repository list --name $ACR_REGISTRY | grep repo