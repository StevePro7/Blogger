Wonderboy Chat GPT
14-Mar-2026

StevePro7
steveprozune@hotmail.com
https://chatgpt.com/c/69b58adc-522c-8333-9d91-484703bad0b4

But I ran out of gas so log in as
SteveProXNA
steven_boland@hotmail.com
https://chatgpt.com/c/69adb604-aa80-8328-a15c-b82e0319fc5d


Q.
Topic: Sega Master System development using devkitSMS https://github.com/sverx/devkitSMS If I upload an old Sega Master System ROM written in pure Z80 assembly e.g. Wonderboy then could you reverse engineer the code - extract the graphic images and rebuild as PNGs or JPGs as 4bpp plus extract the video game music and sound effects stored in vgm or psg format and finally reverse engine the ASM code and re-write in C using APIs from the devkitSMS ? 

Attaching the Wonderboy ROM accordingly


A.
Yes — to a degree. I can help analyze and extract data from a Sega Master System ROM like Wonder Boy, but there are some practical limits to what can be done automatically. A full decompilation and clean C rewrite of an entire commercial game is a large reverse-engineering project that normally takes months for humans. However, many parts can absolutely be extracted and analyzed.

You uploaded the ROM successfully and I can see that:

ROM size: 131,072 bytes (128 KB) — a typical size for early Master System titles.

Below is what I can realistically help you do with the ROM.

1. Graphics extraction (SMS tiles → PNG)

The Master System VDP uses:

8×8 pixel tiles

4 bits per pixel (16 colours)

planar format

tiles stored in ROM or compressed

I can:

✔ Scan the ROM for tile-like data
✔ Decode raw tiles to 4bpp images
✔ Reconstruct tilesheets
✔ Export as PNG

Typical output:

tileset_0.png
tileset_1.png
sprites.png
background_tiles.png

If the graphics are compressed, I can try to identify the compression scheme (many Sega games use simple RLE or custom LZ variants).

2. Palette extraction

SMS palettes are typically stored as:

--BBGGRR

I can locate palette tables and convert them to RGB palettes for the PNG images.

3. Music and sound extraction

The Master System uses the Texas Instruments SN76489 PSG.

Music data inside the ROM usually contains command streams like:

tone
noise
volume
wait

I can try to:

✔ Detect PSG command streams
✔ Convert them to VGM format

Output example:

wonderboy_music_01.vgm
wonderboy_sfx_jump.vgm
wonderboy_sfx_hit.vgm

These can be played with:

VGMPlay

vgmstream

Deflemask

4. Disassembling the Z80 code

I can run a Z80 disassembly of the ROM.

Output example:

wonderboy.asm

However this will initially look like:

ld a,(0xc000)
call 0x1234
jp nz,0x4567

Reverse engineering requires:

identifying functions

mapping RAM usage

naming variables

locating engine subsystems

This is usually an iterative process.

5. Rebuilding the code using devkitSMS

Your link:

devkitSMS
https://github.com/sverx/devkitSMS

After the assembly is understood, we can gradually convert systems to C:

Example transformation.

Original ASM:

ld a,(player_x)
inc a
ld (player_x),a

Converted to devkitSMS C:

player_x++;

Sprite rendering:

SMS_addSprite(player_x, player_y, SPRITE_PLAYER);

Tile loading:

SMS_loadTiles(tiles, 0, 256);
6. Typical final reverse-engineering structure

Projects like this end up with:

/wonderboy-reverse
    main.c
    engine.c
    player.c
    enemies.c
    level.c
    sound.c
    graphics.c

/assets
    tiles.png
    sprites.png
    palettes.pal
    music.vgm

/original
    wonderboy.sms
7. What I cannot guarantee automatically

Some parts require significant manual work:

perfect decompilation to readable C

reconstructing level formats

identifying compression formats

rebuilding the entire build system

But we can get very far.

8. What I can start doing immediately

From the ROM you uploaded I can attempt to:

1️⃣ Extract raw tiles → PNG
2️⃣ Detect palettes
3️⃣ Scan for PSG music data
4️⃣ Produce a Z80 disassembly

If you'd like, I can start by doing the first reverse-engineering pass on the ROM, which would produce:

tiles.png

sprites.png

palette table

first wonderboy.asm disassembly

any detected PSG music streams

Then we can start converting pieces to devkitSMS C code.

Just tell me and I’ll begin with the graphics extraction step.




Q.
let's proceed with the next step recommend Now we should do a smarter graphics extraction, which involves:


A.
It seems like I can’t do more advanced data analysis right now. Please try again later.

I attempted to run the next analysis step (detecting likely sprite/graphics tiles inside the Wonder Boy ROM and generating a filtered tilesheet), but the tool needed to process the ROM and produce the PNG file is temporarily unavailable. Because of that limitation I cannot generate the next downloadable file right now.

When the tool is available again, the next step I planned to run is:

Smart graphics detection pass

The algorithm would:

Decode every 32-byte tile from the ROM (SMS 4bpp format).

Analyze how many colours each tile uses.

Filter tiles that use ≤4–5 colours, which strongly indicates:

sprite graphics

UI tiles

font tiles

Ignore tiles that look like:

program code

tables

compressed data.

Then it would export a new sheet like:

wonderboy_candidate_graphics_tiles.png

That file usually reveals recognizable graphics such as:

Wonder Boy walking frames

enemies

fruits/items

HUD icons

text font

Once we have that sheet, the next reverse-engineering steps become much easier:

Step 2 — Tileset clustering
Group tiles into real tilesets:

player sprites
enemy sprites
background terrain
UI/HUD
font

Step 3 — Palette extraction
Locate the real SMS palette tables and apply them so the graphics appear in correct colours instead of grayscale.

Step 4 — Sprite reconstruction
Rebuild meta-sprites such as:

Wonder Boy player (16×32)
enemies
items

Output example:

wonderboy_player_sprites.png
wonderboy_enemies.png
wonderboy_items.png
wonderboy_hud.png

Step 5 — DevkitSMS asset conversion
Convert the extracted graphics into arrays usable by
devkitSMS and
SMSlib.

Example output:

const unsigned char wonderboy_player_tiles[] = {
    0x00,0x3C,0x42,0xA9,...
};

If you want, when the tool works again I can also do something very powerful for this ROM:

build a full memory map of the Wonder Boy cartridge, showing where Sega stored:

graphics tiles

sprite tables

palettes

level maps

PSG music data

the game engine code

That is usually the key step in a real decompilation project.



OLD CHAT

If you want, after you upgrade I can also help you build a complete automated toolchain for this project, for example:

sms-rom-tools/
   extract_tiles.py
   extract_palettes.py
   extract_psg.py
   disassemble_z80.py
   rebuild_devkitsms_assets.py